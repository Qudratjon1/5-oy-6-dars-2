const name = prompt("Iltimos ismingizni kiriting:");
const spaceId = prompt("Iltimos Space ID ni kiriting:");
const spacePassword = prompt("Iltimos Space Password ni kiriting:");
const userInfo = {
  name: name,
  id: spaceId,
  password: spacePassword
};
console.log(userInfo);
